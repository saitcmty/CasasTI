<%= form_with(model: registration, local: true) do |form| %>
  <% if registration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(registration.errors.count, "error") %> prohibited this registration from being saved:</h2>

      <ul>
      <% registration.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= select("registration", "evidence_id", Evidence.open_evidences.collect {|e| [ e.title, e.id ] }) %>
  </div>

  <div class="field">
    <p>Prueba para la evidencia:
      <span class="side-note">(link hacia elemento compartido)</span>
    </p>
    <%= form.text_field :proof %>
  </div>

  <div class="field justification_field">
    <p>Descripción de lo que hiciste:
      <span class="side-note">(Opcional)</span>
    </p>
    <%= form.text_area :justification %>
  </div>

  <div class="actions">
    <%= form.submit "REGISTRAR" %>
  </div>
<% end %>
